export enum Mode {
  CREATE = "CREATE",
  EDIT = "EDIT",
  VIEW = "VIEW",
}

export enum Layout {
  AUTH = "/auth",
  ADMIN = "/administration",
  DASHBOARD = "/dashboard",
  STATIONNEX = "/stationnex-card",
}

export enum PeriodType {
  NONE = "NONE",
  TODAY = "today",
  YESTERDAY = "yesterday",
  WEEKLY = "weekly",
  MONTHLY = "monthly",
  YEARLY = "yearly",
}
